# Azure Periodic Table

The Azure Periodic Table is a [Next.js](https://nextjs.org/) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app). This application serves as an interactive and intuitive way to explore Azure resources.

## Documentation

The full documentation for this project has been migrated to [MkDocs](https://www.mkdocs.org/). 

You can find the comprehensive documentation including installation, deployment, and contribution guides in the `docs/` directory.

To build and serve the documentation locally, you'll need Python and `pip` installed:

```bash
# Install MkDocs and the Material theme
pip install mkdocs mkdocs-material

# Serve the documentation locally
mkdocs serve
```

Then visit `http://localhost:8000` to view the documentation site.

---
*Dernière mise à jour : 22 avril 2026*
